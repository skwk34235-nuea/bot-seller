<!doctype html>
<html lang="en" class="h-full">
  <%- include('../partials/head') %>
  <body class="min-h-full bg-deep text-white font-sara">
    <div class="auth-shell">
      <%- include('../partials/navbar') %>
      <%- include('../partials/flash') %>
      <main class="mx-auto flex min-h-[calc(100vh-80px)] max-w-7xl items-center px-4 py-12">
        <%- include('../partials/auth-form', {
          mode: 'register',
          eyebrow: 'Create account',
          title: 'Register',
          description: 'Create a customer account and prepare the session for dashboard access and future order workflows.',
          action: '/register',
          submitLabel: 'Create account',
          footerText: 'Already have an account?',
          footerLink: '/login',
          footerLinkLabel: 'Login'
        }) %>
      </main>
      <%- include('../partials/scripts') %>
    </div>
  </body>
</html>
